# Leave empty for the packaged desktop app or same-origin deployment.
# For a separated web deployment, set this to the Python backend origin.
# Both https://api.example.com and https://api.example.com/api are accepted.
NEXT_PUBLIC_API_BASE_URL=

# Enable MSW only for UI development without a running Python backend.
NEXT_PUBLIC_USE_MOCK=false
